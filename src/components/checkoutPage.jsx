import { Box, Typography } from "@mui/material";

const CheckoutPage = () => {
  return (
    <Box sx={{ padding: 2, textAlign: "center", marginTop: 12 }}>
      <Typography variant="h6" gutterBottom>
        Will create checkout and order summary section in future as task does
        not mentions it!
      </Typography>
    </Box>
  );
};

export default CheckoutPage;
